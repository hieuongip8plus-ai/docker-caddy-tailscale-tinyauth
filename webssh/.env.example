# =============================================================================
# webssh/.env.example — browser terminal backed by ttyd + tmux
# =============================================================================
#
# Docs:
#   - https://github.com/tsl0922/ttyd
#   - https://github.com/tmux/tmux/wiki
#
# Profiles: webssh | full
# =============================================================================

# DOMAIN
#   Default WEBSSH_HOSTS = http://ttyd.${DOMAIN}, http://webssh.${DOMAIN}
DOMAIN=example.com

# WEBSSH_HOSTS
#   Caddy site addresses. Use http:// because TLS terminates at Cloudflare/Tailscale.
#   The service is protected by Tinyauth.
#   Both hostnames route to the same webssh container.
WEBSSH_HOSTS=http://ttyd.example.com, http://webssh.example.com

# Session behavior:
#   webssh starts `tmux new-session -A -s webssh`.
#   Closing the browser detaches; reopening attaches to the same tmux session.
#   The repository root is mounted at /workspace, including hidden files.
